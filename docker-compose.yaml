services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - 80:80
    depends_on:
      - backend

  backend:
    build:
      context: ./spring-backend
      dockerfile: Dockerfile
    environment:
      MONGO_URI: mongodb+srv://solutecdvd:ELqaCsoX3nSGNYwK@solutec-dvd.y59aggc.mongodb.net/?retryWrites=true&w=majority&appName=Solutec-DVD
    ports:
      - 8080:8080
